<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Product</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <main class="container narrow">
    <header class="header">
      <h1>Edit Product</h1>
      <a class="link" href="/products">Back</a>
    </header>

    <form class="form" action="/products/<%= product.id %>" method="POST" enctype="multipart/form-data">
      <label class="field">
        <span>Name</span>
        <input required name="name" type="text" value="<%= product.name %>" />
      </label>

      <label class="field">
        <span>Price</span>
        <input required name="price" type="number" step="0.01" min="0" value="<%= product.price %>" />
      </label>

      <label class="field">
        <span>Description</span>
        <textarea name="description" rows="3"><%= product.description || '' %></textarea>
      </label>

      <label class="field">
        <span>Current image</span>
        <% if (product.imageUrl) { %>
          <img class="thumb" src="<%= product.imageUrl %>" alt="<%= product.name %>" />
        <% } else { %>
          <span class="thumb placeholder">—</span>
        <% } %>
      </label>

      <label class="field">
        <span>Replace image (optional)</span>
        <input name="imageFile" type="file" accept="image/*" />
      </label>

      <div class="actions">
        <button class="button" type="submit">Update</button>
      </div>
    </form>
  </main>
</body>
</html>
